# Local Secrets

Put sensitive values here on each machine. This folder is ignored by git except `.gitkeep`.

Required for mail:
- Create `.secrets/mail_password.txt`
- Put only the SMTP app password in the file (single line)
